const BASE_URL = 'https://constellation-webservice.herokuapp.com';

const USER_PROFILE_URL = `${BASE_URL}/user-profile`;
const AUTH_URL = `${BASE_URL}/auth`;
const TRANSACTION_URL = `${BASE_URL}/transaction`;

export {BASE_URL, USER_PROFILE_URL, AUTH_URL, TRANSACTION_URL};
